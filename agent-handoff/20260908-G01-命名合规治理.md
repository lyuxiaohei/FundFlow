# 20260908-G01-命名合规治理

> **状态**：待执行
> **拍板**：2026-09-08；**迁档**：2026-09-12（自原 `AGENT-交接文档.md` §二原样迁出，该文件已冻结；路径与回写目标按下述迁移注记适配，其余内容保持原貌）
> **项目根（全部相对路径以此拼接，cwd 不可依赖）**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow`
> **失败清单（绝对路径）**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/goal-20260908-命名治理-失败清单.md`
> **执行方式**：①并入 `20260912-G02-原型演示前完善.md` 的 T0 执行（推荐，一次跑完会前全部事项）；或②独立 dispatch（需另配 /goal 条件，按本档执行）
> **上下文基线**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/_AGENT基线.md`（先读全文再动手）

> **迁移注记（2026-09-12，共四处适配）**：
> 1. 原文相对路径中的 Windows 根 `D:\工作台-吕道远\5-【ACTIVE】投资项目管理\` 统一改为上述 macOS 项目根，分隔符 `\` 改 `/`；
> 2. 原文「交接文档 1.4 口径与纪律」改指 `_AGENT基线.md` 第六节；
> 3. 原文收尾「任务节状态/台账/1.2 基线回写」的回写目标改为：本档头部状态 + `_索引.md` G01 行 + `_AGENT基线.md` 三、现状基线；
> 4. 2.0 前置校验追加第 4 条 mtime 互斥（v7 资源互斥硬性要求，原文无）。

---

## 背景（2026-09-08 全站命名合规扫描）

扫描发现 P0 四项 + P1 四项执行项、P2 七项待议。本任务执行 P0/P1，P2 原文落 A13「待议项」节供人拍板。
无人值守执行：不需要也不得向用户提问；决策按本任务默认决策表；拿不准按最简合理默认执行并在收尾报告「默认决策命中」节注明。

## 2.0 前置校验（先于一切，防重复/并发；任一不符 → 写失败清单〔路径见头部〕注明后收尾，禁止重做）

| # | 标记（项目根相对） | 期望值 |
|---|---|---|
| 1 | `P3-R01-原型说明.md` 版本行 | 含 `V1.5`（含 V1.6+ = 已执行/并发 → 停） |
| 2 | `P2-R01/P2-R01-A13-需求调整记录-20260908.md` | 不存在 |
| 3 | `agent-handoff/goal-20260908-命名治理-失败清单.md` | 不存在 |
| 4 | （迁移补强 20260912）`P3-R01-原型/` 与 `agent-handoff/` 最近文件 mtime | ≥30 分钟（本档与失败清单豁免），不符=并发 → 停 |

校验通过后创建失败清单 `agent-handoff/goal-20260908-命名治理-失败清单.md`（表头：时间/任务/对象/原因/处置），后续跳过项实时追加。

## 2.1 任务组 T1~T9（改动范围 = `P3-R01-原型/` 21 个 F 页 + 根层文档；99-归档／_p3_backup*／A 系列历史记录／.research 一律不改）

| # | 任务 | 改动点 | 不动项 |
|---|---|---|---|
| T1 | F20 视图标签行业化（P0） | 视图页签与页内文案：我的待审→**待我审批**、抄送我→**抄送我的**、已审→**我已审批**（钉钉口径） | 「我发起的」（本已合规）；弹窗标题「审批处理」「发起审批」 |
| T2 | F00 用词对齐（P1） | 「我的申请」→**我发起的**（约 2 处，与 F20 统一） | 我的待办／停滞提醒（钉钉有「待办」先例，保留） |
| T3 | F09 合规用词（P1） | 「LP 认定」→**合格投资者认定**（弹窗标题+正文，约 7 处） | 行内两字按钮「认定」；D02 英文实体/字段 |
| T4 | F21 开发词出清（P0） | 按默认决策表映射替换字典族可见文字（现约 70 处） | id/class/JS 变量含 dict 字样；「原『字典管理』」历史更名注记 |
| T5 | F22/F23 黑话出清（P0） | 页头 tip「数据源自项目库，按段切片（投后段/退出段）」→「数据源自项目库（进入投后管理的项目）」/「（进入退出与分配的项目）」 | 文档层「段列表」内部速记（A13 注明口径） |
| T6 | F01 状态名去圈号（P1） | 「已投（⑨后）」→**已投（出资后）**（1 处） | 其余 ①~⑫ 带圈形态全保留（C 类）；「未投归档·GP/·LP」保留；「不可回流管道」文案不改（P2 待议） |
| T7 | F02/F03 定版词落地（P0） | `<title>`、dtabs 页签、卡片/区块标题中的「基本信息」→**项目概况**（A08 已定版未落地，三变体并存） | 表单字段标签与说明句中指"基础字段区块"语义的保留原文并计入报告；F08「基金概况」不动 |
| T8 | 四文件重命名（P1） | 见 2.3 重命名表；全站 go()／页签条／D04 导航图／D02 标注 json 内旧文件名引用同步改写 | 文件内容不改（纯改名）；99-归档内旧副本不动 |
| T9 | 收尾落档 | A13 记录（含 P2 待议七项清单）+ 原型说明 **V1.6** + PRD 10.2 + D03 同步 + `_索引.md` 与 `_AGENT基线.md` 回写 | 格式沿用 A07~A12 |

## 2.2 P2 待议七项（不执行，原文写入 A13「待议项」节）

1. 带圈序号 ①~⑫ 大面积进 UI 正文形态（F01 泳道列头／F19/F21 原因项／F02/F03 弹窗提示）
2. 放弃原因「看 BP 后不看」口语化（D01 初版占位，本就待业务清单替换）
3. F01 帮助文案「不可回流管道」（已删旧菜单「投前管道」遗产）
4. 行内动作「停滞处置」（"处置"语义偏风险处置）
5. F08 页签「出资记录」×8 与 F10 弹窗「新增出资记录」（A10 有保留决策，建议重审）
6. 阶段名与模块名撞名（⑩投后管理／⑫清算注销=菜单组名，泳道列头与侧栏同屏歧义）
7. D03 菜单树根「投资项目库」未随系统更名（文档层）

## 2.3 默认决策表

| 决策项 | 默认值 |
|---|---|
| T4 字典族映射 | 字典项→**数据项**；新增/编辑字典项→新增/编辑数据项；字典项名称→数据项名称；所属字典→所属数据分类；字典分类→**数据分类**；分组字典→分组数据；末级原因字典→末级原因数据；导出当前字典全部条目→导出当前数据全部条目；「原字典管理／原『字典管理』」历史注记原样保留 |
| T4 残留判定 | 替换后 F21 内 `字典(?!管理)` 计数=0；表单 label／placeholder／JS 可见字符串一并替换；JS 标识符（变量/函数/id/class）不动 |
| T1/T2/T3/T6/T7 计数纪律 | 每个旧串替换前先精确计数（本节所写"约 N 处"仅为量级参考，以实测计数为准），替换数=实测计数才过断言；标签在 JS 模板字符串内的一并替换，替换后该页必跑 Playwright（引号丢失教训） |
| T7 形态界定 | 只改三种形态：`<title>` 文本、dtabs 页签文字、卡片/区块标题文字；其余「基本信息」保留原文，逐条计入收尾报告「保留项」 |
| T8 重命名表 | `项目库/P3-R01-F02-项目详情-基本信息.html`→`P3-R01-F02-项目详情-项目概况.html`；`LP 管理/P3-R01-F09-LP名录.html`→`P3-R01-F09-LP台账.html`；`LP 管理/P3-R01-F10-出资记录.html`→`P3-R01-F10-LP出资.html`；`投后管理/P3-R01-F11-投后时间线.html`→`P3-R01-F11-投后事项.html` |
| T8 移动纪律 | python shutil.move 内嵌中文绝对路径；目标已存在同名→跳过记失败清单禁覆盖；移动后全站（21 F 页+D04）扫描旧文件名字符串改写（go()/href/tab），每个旧文件名替换带出现计数断言 |
| T8 标注 json | D02 json 内若有旧文件名引用→同步精确替换；selector 为 HTML 子串与文件名无关，不重跑注入；json 结构校验失败→跳过留档 |
| T9 版本与编号 | 原型说明 V1.5→V1.6（版本行+页面清单四文件名+UI 规范行补记命名治理）；A 系列下一号 **A13**；D03 菜单树根不改（P2 待议第 7 项） |
| 文档同步范围 | D03：2.2 页签行「基本信息/材料库」→「项目概况/材料库」、P02/P09/P10/P11 页面名同步；PRD 10.2 加 A13 行；原型说明页面清单四个文件名行同步 |
| git 收尾 | 一次 `git add -A && git commit -m "A13 命名合规治理 P0/P1"`（**不 push**；git 失败不阻塞完成判定，记入报告即可） |
| 兜底自愈 | 任一精确串计数不符→该串跳过记失败清单（对象+实际计数+期望计数），继续下一项；同一文件多串独立断言互不阻塞 |

## 2.4 失败处理策略（无人值守铁律）

任何 assert 失败／计数不符／结构异常：跳过该项→记失败清单（时间/任务/对象/原因/处置）→继续下一项。禁止卡住等待、禁止对不确定结构猜测性改写、禁止静默吞掉。收尾报告必须含失败清单结论（可为空）。

## 2.5 纪律（违反=返工）

- `_AGENT基线.md` 第六节 口径与纪律逐条适用（术语唯一源／C 类词永不改／不动项／防并行覆盖／落档）
- 按任务动作类型反查教训全集：**文本替换类**（精确串+期望计数断言+幂等，业务页面禁整文件重写）；**文件重命名类**（python 内嵌中文绝对路径、move 禁覆盖同名、引用同步改写+死链复扫）；**JS 字符串类**（改后该页 Playwright 必跑，防引号丢失语法坏整页功能挂）；**Playwright 验证类**（file:// 用 pathlib.as_uri、每页新建 page、双监听 pageerror+console.error 防监听器累积）
- 本次不新增整站备份：纯文本精确替换+4 个文件重命名，项目根 2026-09-08 起已 git 纳管可回溯

## 2.6 验证门（全部满足才算完成，证据形态逐条点名，输出贴进对话）

1. **旧词清零**：python 正则断言 21 F 页，证据＝一行 `旧词清零：我的待审=0/独立已审=0/独立抄送我=0/LP 认定=0/字典(非历史注记)=0/按段切片=0/已投（⑨后）=0/我的申请=0/基本信息(页签与title形态)=0`（"独立"=排除新词内子串，如「我已审批」含"已审"、「抄送我的」含"抄送我"）
2. **新词落位**：证据＝一行 `新词落位：待我审批≥1/抄送我的≥1/我已审批≥1/F00我发起的≥1/合格投资者认定≥1/数据项≥1/项目概况(F02与F03各≥1)/已投（出资后）=1`
3. **重命名**：证据＝一行 `重命名=4；旧文件名全站残留引用=0；四个新文件 os.path.exists=真×4`
4. **死链扫描**：python 扫 22 个 html（21 F 页+D04）全部 go() 与 sys-pop 目标逐一 exists，证据＝`死链 0 / 链接总数 N`
5. **Playwright**：21 页逐页，证据＝逐页 `PASS <文件名>` 清单 + 末行 `PASS 21/21, FAIL 0, JS错误 0`
6. **文档落档**：grep 断言四行——`A13 存在且含待议项：真`／`原型说明版本行含 V1.6：真`／`PRD 10.2 含 A13 行：真`／`D03 含项目概况页签：真`
7. **失败清单**：证据＝结论行原文（空清单给「无失败项」）

## 2.7 收尾（回写三件+git 批次为硬性）

1. 本档头部状态改「✅ 已完成」+ 执行记录节（证据摘要+默认决策命中+失败清单结论）
2. `_索引.md` G01 行挂 A13 与 commit 哈希；`_AGENT基线.md` 三、现状基线同步（四文件名／字典族口径／F20 视图名／版本 V1.6）
3. A13 含「待议项」七条清单；git 收尾提交（不 push，失败不阻塞）；失败清单结论行贴出

> 迁移注记：原文回写目标「AGENT-交接文档.md 任务节状态/台账/1.2 基线」因该文件冻结，改为上述三件（2026-09-12）。
