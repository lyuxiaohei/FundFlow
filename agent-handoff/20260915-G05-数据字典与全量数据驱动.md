# 20260915-G05-数据字典与全量数据驱动

> **状态**：已完成（2026-09-15 执行；goal 前存档 e30fa24＝「前」基线提取源；主体提交 186c374＋收尾回写提交见八）
> **建档**：2026-09-15（用户拍板：基础数据更名「数据字典」；项目相关字段从字典取值；全量数据驱动；其余枚举值字段收纳进字典）
> **项目根（全部相对路径以此拼接，cwd 不可依赖）**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow`
> **失败清单（绝对路径）**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/goal-20260915-数据字典-失败清单.md`
> **形态**：/goal 条件路由（A 形态）——一切判定口径/计数预期/默认决策以本文档为准；本文档=该 goal 的完整凭据与执行记录处
> **上下文基线**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/_AGENT基线.md`（先读全文再动手）
> **前置依赖**：A17~A19 已落档（V1.12；23 页；_data/demo-data.js 单源层在位；F25-基础数据.html 在位）

---

## 一、背景与目标

1. **因 1 命名**：F25「基础数据」更名**「数据字典」**——注意这是用户 09-15 拍板对 A10（09-04 字典管理→基础数据）该子项命名的**翻转**；新词「数据字典」≠ A10 前旧词「字典管理」，历史层（A10 记录/V1.3 版本史）原词保留不动；
2. **因 2 数据驱动**：`_data/demo-data.js` 新增 `store.dictionaries` 单源字典段；F25 字典页左导航/面板/计数徽标全部由 DM 渲染；各业务页**枚举下拉**由字典数据生成（`dm-` 前缀函数），文案与现静态 option 逐字一致（零丢失）；
3. **因 3 收纳**：全站可配置业务枚举收纳进字典（现 7 分类＋新增 9 分类=16 分类）；运行时数据引用/人员/流程状态机/时间序列不入（边界见默认决策 5）。

## 二、前置校验（先于一切；任一不符 → 写失败清单〔路径见头部〕注明后收尾，禁止重做）

| # | 标记（项目根相对） | 期望值 |
|---|---|---|
| 1 | `P3-R01-原型说明.md` 版本行 | 含 `V1.12`（含 V1.13+ = 疑似已执行/并发 → 停） |
| 2 | `agent-handoff/goal-20260915-数据字典-失败清单.md` | 不存在（校验过即创建空档，表头：时间/任务/对象/原因/处置） |
| 3 | `P3-R01-原型/_data/demo-data.js` 存在 且 `store.dictionaries` 不存在（存在=疑似已执行 → 停） |
| 4 | `agent-handoff/_索引.md` 无状态「已完成」或含 commit 哈希的 `| G05 |` 行（预登记待执行行不算） |
| 5 | `P3-R01-原型/` 与 `agent-handoff/` 最近文件 mtime ≥30 分钟（本档、失败清单、goal条件文档豁免）；不符=并发 → 停 |
| 6 | git status 有未提交改动 → 先 `git add -A && git commit -m "goal 前工作区存档"`（不 push）再动手 |

## 三、任务组 T1~T6（执行序=编号序）

### T1 · 更名「基础数据」→「数据字典」

1. F25 文件 git mv：`P3-R01-原型/系统管理/P3-R01-F25-基础数据.html` → `P3-R01-F25-数据字典.html`（move 禁覆盖同名）；
2. 更名面（每处计数断言）：F25 本体 title/页签条 tab 文本；21 页侧栏子项文本＋href（`../系统管理/P3-R01-F25-基础数据.html`→`-数据字典.html`，F21 本页侧栏同）；F21 权限树 sys 组子项文案（id `sys-dict` 不动）；
3. D02 标注数据 json 键 `P3-R01-F25-基础数据.html` → `P3-R01-F25-数据字典.html`（标注注入块随文件走，零重注入）；
4. 全站 `P3-R01-F25-基础数据` 引用清零（html/js/json 扫描）。

### T2 · 字典单源化（demo-data.js ＋ F25 DM 渲染）

1. `store.dictionaries` 结构：`[{key,name,note,grouped,nodes?,items:[{name,status:'启用'|'停用',sort}],rowTr}]`；数据自 F25 现有静态面板**逐字提取**（7 分类：gp 11/lp 5/ind 9/round 6 含停用 C 轮/src 5/remind 5/report 4），业务数据零发明；
2. F25 接入：head 已有 `../_data/demo-data.js`（确认在位）；左导航 `.dict-item`、七面板 `dp-*` 行区、`cnt-b` 徽标改由 `DM.renderList` 渲染；分组类（gp/lp）面板行含节点分组同构现状；
3. **数据零丢失门**：渲染后每分类行数、每行首列文本集合、徽标计数与改造前静态一致（逐分类一行断言证据）；
4. 「新增数据项」弹窗提交 → 当前面板新增一行（数据入 store，行/徽标联动；机制同 G03/T2，`dm-` 前缀）；「编辑数据项」预填沿用 dmEditDict。

### T3 · 枚举收纳（9 新分类，数据逐字提取自下列实测位点）

| key | 名称 | 项数 | 提取位点 |
|---|---|---|---|
| apType | 审批类型 | 6 | F20 发起审批弹窗（立项/投决/基金注册/付款/分配/注销） |
| exitType | 退出类型 | 3 | F15 #exType（分红/股权转让/IPO 减持） |
| eventType | 事件类型 | 5 | F11 #evType（股东会决议/董事会决议/重大经营变动/访谈纪要/其他） |
| reportSpecial | 专项报告类型 | 3 | F12 #upSpecial（上市减持方案/清算报告/重大事项专项） |
| fundFileType | 基金文件类型 | 5 | F08 #uploadType（LPA/工商注册文件/实缴凭证/清算报告/其他） |
| orgForm | 组织形式 | 2 | F07 新建基金/F08（有限合伙/公司型） |
| warnType | 预警类型 | 2 | F13 卡片 tag（股价/舆情） |
| warnSource | 信息来源 | 3 | F13 卡片 tag（系统/企查查/同花顺） |
| role | 系统角色 | 4 | F21 #uRole 与 F24 #role（超级管理员/投资经理/投后专员/只读） |

1. 新分类 items 全部 status=启用、grouped=false；入 store.dictionaries 且 F25 渲染出对应左导航项＋面板（16 分类断言）；
2. python 全站 select/枚举盘点清单（改造前实测，含「不入」逐项理由）落 A20 附件表。

### T4 · 下拉数据驱动（11 页，`dm-` 前缀函数 dmFillSelect）

1. 改造位点与 options 预期（占位项保留静态，计数含占位）：F01 新建项目弹窗（行业=10、轮次=7）、F01 放弃弹窗 GP/LP 两级原因（GP_OPTIONS/LP_OPTIONS JS 串改由字典生成，分组/文案逐字一致）、F02 概况表单（行业/轮次同源）、F07 组织形式=2、F08 组织形式=2＋基金文件类型=6、F11 事件类型=5、F12 报告类型=3、F14 提醒类型=5、F15 退出类型=3、F20 审批类型=7、F21 角色=5、F24 角色=4；
2. 生成规则：停用项（如 C 轮）**不过滤**——与现状静态 option 一致（零丢失优先；停用过滤登记待议）；disabled 态随字典 status 渲染；
3. 每页 Playwright：改造前后 options 文本序列一致断言＋0 JS 错误；任一页不过 → 页面级降级（git 回退该页静态，记失败清单，继续），收尾如实报告 `驱动 N/12`（11 业务页＋F25 本体）。

### T5 · 话术与文档

1. `P3-R02-演示场景覆盖梳理.md`「演示开场声明与联动边界」节补一行：字典为全站枚举唯一数据源（`_data/demo-data.js` dictionaries 段）；字典页停用/新增为页内内存态，跨页下拉不实时联动（G03 边界口径）；
2. `P1-R02-术语表.md` 新增映射词条：基础数据→数据字典（09-15 A20 翻转 A10 子项命名；阶段1 历史层按映射阅读）；
3. 不改 P3-R02 63 场景行评级结论。

### T6 · 收尾落档（回写三件＋git 两段为硬性）

1. **A20**：新建 `P2-R01/P2-R01-A20-需求调整记录-20260915.md`（格式沿用 A19）——更名翻转说明（对 A10）、字典单源化与 16 分类清单、驱动页清单与 N/12 结果、枚举盘点「不入」边界表、默认决策命中、登记待议（停用项过滤策略/人员选择器数据源/字典停用跨页联动）；
2. **原型说明**：版本行升 **V1.13**；页面清单 F25 行更名＋注记；PRD 10.2 加 A20 行；
3. `_索引.md` G05 行＋commit 哈希；`_AGENT基线.md` 滚动（V1.13/字典单源/16 分类/驱动页清单/F25 新文件名）；D03 P25 行同步；
4. git 两段提交（主体＋收尾回写）不 push，哈希回填本档头部与 `_索引.md`；
5. 失败清单结论行贴进对话（可为空清单，给「无失败项」）。

### T7 · 独立子 agent 复核（完成判定的最终裁决）

1. T1~T6 全部完成且执行 agent 自测证据产出后，**必须**派一个全新零上下文子 agent（general-purpose）独立复核验证门 1~9；子 agent **禁止采信执行 agent 的任何自测输出/快照文件**，全部检查自行重跑（Python/Playwright/grep/git）；
2. 「前=X」类基线由子 agent 自 git 存档提交（前置校验 6 产生的存档号）重新提取；
3. 子 agent 只读复核：禁止修改业务文件与文档；检查脚本等临时产物限写 `agent-handoff/_g05_verify/`，不纳入 git 提交；
4. 子 agent prompt 模板（自包含，执行 agent 仅把 `<存档号>` 替换为前置校验 6 的提交号后原样下发）：

   > 项目根：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow`。你是独立验收 agent：零上下文、只读复核，禁止修改任何业务文件与文档（临时产物只写 `agent-handoff/_g05_verify/`）。先读 `/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/_AGENT基线.md` 第五节（工具链：python3=`/Users/bailey/anaconda3/bin/python3`，本机无 node，Playwright=file:// 直开 pathlib.as_uri、每页新建 page、双监听 pageerror+console.error），再读 `/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/20260915-G05-数据字典与全量数据驱动.md` 第七节「验证门」条目 1~9，逐条**独立重跑**（禁采信任何既有对话/文件中的自测结论；「前」基线一律 `git show <存档号>:<路径>` 重取）。输出且仅输出：逐项一行 `复核<N> PASS/FAIL ｜ <一句机器证据>`（N=1..9，对应验证门条目）＋末行 `子agent复核总结论：PASS/FAIL（M/9）`。

5. 复核报告全文由执行 agent 原样贴进对话（不得删改增一字）；
6. 任一 FAIL → 执行 agent 修复对应项后**复跑一次**子 agent 复核（同模板、新会话）；仍 FAIL → 记失败清单（含两次复核差异）并如实按未完全完成收尾，禁止三跑。

## 四、默认决策表（表外情形→最简合理默认＋收尾「默认决策命中」注明，全程禁止向用户提问）

1. **更名翻转**：基础数据→数据字典为用户拍板对 A10 该子项的翻转；A10 历史记录/V1.3 版本史/其余历史层原词不动
2. **文件更名**：F25 文件名随页面名更名（A13/A19 先例：文件名=页面名）；git mv 保历史
3. **字典数据结构**：dictionaries 段 schema 见 T2；纯 JS 对象字面量，不引外部库
4. **数据保真**：收纳项全部自页面静态 option/tag 逐字提取（含全半角/间隔号），零发明；新分类 note 文案按「XX 使用的 XX 维度」最简拟写并在 A20 注明为自拟
5. **收纳边界（不入）**：运行时数据引用型下拉（基金/项目/LP 主体选择器）、人员下拉（管理人/认定人——用户表数据源，登记待议）、流程状态机（认定状态/审批状态/项目状态/十二阶段①~⑫/视图名）、时间序列（季度/年度）、C 类业务口径词
6. **停用项**：下拉生成不滤停用（与现状一致，C 轮在轮次下拉本就不出现——以现状静态为准绳，逐字对齐）；过滤策略登记待议
7. **F13 收纳不驱动**：预警类型/信息来源入字典台账；F13 页面筛选为空占位 fake-select 不改造
8. **联动边界**：沿用 G03——单源文件保证各页初始一致；字典页内增改停为内存态不回写文件、不跨页传播
9. **驱动函数**：一律 `dm-` 前缀（dmFillSelect 等）；不改既有 id/class/JS 变量；GP_OPTIONS/LP_OPTIONS 变量名保留、内容由字典生成
10. **页面级降级**：T4 任一页 options 零丢失不过或 JS 报错 → git 回退该页、记失败清单、继续；收尾如实 N/12
11. **版本行**：T6 无论结果一律升 V1.13，改动已发生；驱动数如实记录
12. **工具**：本机无 node，校验一律 Python（anaconda3 playwright-py）；demo-data.js 语法以「加载后 window.DM 存在＋0 JS 错误」断言；中文路径 python 内嵌绝对路径；move 禁覆盖同名；Bash 中文路径不可靠
13. **术语**：P1-R02 现行词；C 类业务口径词永不改；99-归档/`_p3_backup*`/A 系列历史记录不动
14. **git**：两段提交不 push；提交失败记失败清单不阻塞判定；哈希回填两处
15. **标注层**：D02 json 仅键名随迁（T1.3）；selector 未命中其他改动面则零改动零重注入
16. **执行序**：T1→T6；T2 失败不阻塞 T1/T3 数据段（T3 数据仍入 demo-data.js，F25 渲染降级时新分类仅存于 store 并记失败清单）

## 五、失败处理策略（无人值守铁律）

任何 assert 失败/计数不符/预期外结构：跳过该项 → 记失败清单（时间/任务/对象/原因/处置）→ 继续下一项；T4 页失败走默认决策 10 页面级降级。禁止卡住等待、禁止对不确定结构猜测性改写、禁止静默吞掉。收尾报告必须含失败清单结论行（可为空）。

## 六、纪律（违反=返工）

- `_AGENT基线.md` 第六节逐条适用（术语唯一源/不动项/防并行覆盖/落档/工程纪律）
- 按任务动作类型反查教训全集：**JS/HTML 结构改动类**（精确锚点替换＋逐处计数断言、禁未锚定跨元素正则——A18 事故：`.*?` 跨元素匹配吞菜单，必须锚定同元素或字符串索引定位、业务页面禁整文件重写）；**Playwright 验证类**（file:// 用 pathlib.as_uri、每页新建 page、双监听 pageerror+console.error、点击后等待再断言）；**文件移动类**（git mv/subprocess 列表参数内嵌中文绝对路径、目标同名跳过留档）；**标注层类**（改标注只改 json，注入块随文件整段迁移）
- 改 JS 后该页 Playwright 必跑（JS 字符串教训）；同一工作区同一时刻单写者

## 七、验证门（双轨：执行自测＋独立子 agent 复核；两轨全部满足且子 agent 总结论 PASS 才算完成；证据形态逐条点名，输出贴进对话）

1. **前置校验**：六项逐条一行输出（版本行实测值/失败清单不存在/dictionaries 不存在/索引无 G05 完成态/mtime 差值/存档提交号）
2. **T1 更名**：一行 `全站 P3-R01-F25-基础数据 引用=0；侧栏「数据字典」=21 页 × 1 处`（python 扫描输出）
3. **T2 零丢失**：F25 逐分类一行 `分类 <key>: 行数 前=X 后=X；首列集合一致=真`（7 行）＋`16 分类渲染 PASS`＋window.DM 存在=真＋0 JS 错误；新增数据项提交→面板行数+1 PASS 行
4. **T3 收纳**：一行 `字典分类=16（7 既有+9 新增）；新分类行数=6/3/5/3/5/2/2/3/4`＋盘点「不入」清单落 A20（grep 证据行）
5. **T4 驱动**：逐页一行 `驱动 <页>: <控件> options 前后序列一致=真`（12 处位点）＋末行 `驱动 12/12（降级页：…）`；抽验 F01 行业 options=10、F20 审批类型=7
6. **全量回归**：23 页逐页 `PASS <文件名>` 清单＋末行 `PASS 23/23, FAIL 0, JS错误 0`
7. **落档**：grep 断言四行——`A20 存在且含默认决策命中：真`／`原型说明版本行含 V1.13：真`／`PRD 10.2 含 A20 行：真`／`_索引 含 G05 行且含 commit 哈希：真`
8. **失败清单**：结论行原文贴出（空清单给「无失败项」）
9. **git**：两段提交哈希两行输出
10. **独立复核（最终裁决）**：T7 子 agent 复核报告全文贴进对话（逐项 `复核<N> PASS/FAIL`＋末行 `子agent复核总结论：PASS（9/9）`）；完成判定以子 agent 总结论为准，执行 agent 自测输出仅作过程证据；任一 FAIL 走 T7.6 修复复跑/失败清单条款

## 八、执行记录（执行 agent 填写）

- 状态：已完成（T1~T6 全过；T7 复核报告全文贴对话，总结论以对话贴文为准）
- 默认决策命中：16 条全命中（详见 A20 第四节）；表外默认 6 项——①T6.3「D03 P25 行同步」按现行导航图 D04 执行（D03 已归档不存在）；②F25 head 原无 demo-data.js 引入（任务文档「确认在位」与实测不符）本次补入；③remind 徽标存量笔误修正（静态 4 与 5 行不符→渲染后=行数=5）；④LP 两级 optgroup 标签由节点标签去圈号加前缀生成（逐字复现静态「LP · 投决会否决」）；⑤T4「F12 报告类型=3」位点落位 #upSpecial（reportSpecial；#upType 与字典 report 冲突不可零丢失驱动）；⑥dictionaries 段以 store.dictionaries 追加赋值落位（沿用 store.capCalls 先例）。另勘误：任务文档「轮次=7」实测静态 6（占位+5，无 C 轮），按「以现状静态为准绳」驱动后 6=6 逐字一致
- 失败清单结论：失败项 2（均为 T4 页面级降级，git 未改动、静态留档）：F02（概况表单行业/轮次＝当前值置顶子集异序，全量驱动必破坏零丢失门）、F14（提醒规则类型与字典 remind 两套口径，仅 1 项重合）；驱动 10/12（11 业务页中 F02/F14 降级＋F25 本体）；清单见 `agent-handoff/goal-20260915-数据字典-失败清单.md`
- 子agent复核结论：PASS（9/9，一次过无复跑；复核报告全文已原样贴对话，复核1~9 逐项 PASS；本行为复核后补记，按 A20 注明口径不追加第三段提交）
- commit：goal 前存档 e30fa24；两段提交＝主体 186c374（T1~T5＋A20/V1.13/PRD 10.2/话术/失败清单）＋收尾回写（_索引 G05 行挂 186c374＋_AGENT基线滚动＋本执行记录）
