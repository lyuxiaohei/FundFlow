# 20260914-G03-演示闭环修复

> **状态**：待执行
> **建档**：2026-09-14（背景=09-14 演示闭环诊断四因；范围=道远拍板「全量含数据驱动」）
> **项目根（全部相对路径以此拼接，cwd 不可依赖）**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow`
> **失败清单（绝对路径）**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/goal-20260914-演示闭环-失败清单.md`
> **形态**：/goal 条件路由（A 形态）——一切判定口径/计数预期/默认决策以本文档为准；本文档=该 goal 的完整凭据与执行记录处
> **上下文基线**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/_AGENT基线.md`（先读全文再动手）
> **前置依赖**：G02 已执行（原型现状=V1.9，A13~A16 已落档，22 页 Playwright 全绿）
> **硬时限语境**：用户演示临近（09-12 时称数日内）——本 goal 单次跑完；页面级降级条款保演示安全（任何接入失败页回退静态，不带病上线）

---

## 一、背景与目标（诊断结论→修复映射）

09-14 诊断「演示无法闭环」四因，本 goal 逐因处置：

1. **因 1 静态快照、动作无状态变化**（核心）→ T1 单源数据层＋T2 页内动作真联动：操作后页面可见变化（行迁移/徽章更新/行数增减），跨页一致性由单源渲染保证；跨页动作传播仍为讲解词（浏览器 file:// 无跨页会话，登记为已知边界，见 T5 话术）；
2. **因 2 alert 查看类死端 30+**（F20 审批单详情 ×8、F19 未投归档详情 ×9、F15 编辑批次 ×3、F09 资质材料 ×4、F08 文件预览 ×4、F11 附件 ×2、F12 报告预览 ×1、F16 付款凭证 ×1、F01 处置标注 ×1）→ T3 全部改真弹窗（复用各页现有 `.modal` 骨架）；
3. **因 3 计数互溯断**（F18 漏斗 36…5 vs F01 仅 10 行；F19 放弃明细 10 项目中 8 个 F01 不存在）→ T4 数据补齐＋计数由单源推导＋口径注记；
4. **因 4 分支/角色叙事无载体** → T5 演示开场声明与联动边界话术落档；F24 角色真联动登记待议（不在本 goal）。

## 二、前置校验（先于一切；任一不符 → 写失败清单〔路径见头部〕注明后收尾，禁止重做）

| # | 标记（项目根相对） | 期望值 |
|---|---|---|
| 1 | `P3-R01-原型说明.md` 版本行 | 含 `V1.9`（含 V1.10+ = 疑似已执行/并发 → 停） |
| 2 | `agent-handoff/goal-20260914-演示闭环-失败清单.md` | 不存在（校验过即创建，表头：时间/任务/对象/原因/处置） |
| 3 | `P3-R01-原型/_data/demo-data.js` | 不存在（存在=疑似已执行 → 停） |
| 4 | `agent-handoff/_索引.md` | 无状态为「已完成」或含 commit 哈希的 `| G03 |` 行（预登记待执行行不算） |
| 5 | `P3-R01-原型/` 与 `agent-handoff/` 最近文件 mtime | ≥30 分钟（本档、失败清单、goal条件文档豁免）；不符=并发 → 停 |
| 6 | git status | 有未提交改动 → 先 `git add -A && git commit -m "goal 前工作区存档"`（不 push）再动手 |

## 三、任务组 T1~T6（执行序=编号序）

### T1 · `_data/demo-data.js` 单源数据层＋列表/统计页接入（因 1/因 3 根治）

1. 新建 `P3-R01-原型/_data/demo-data.js`：全局 `window.DM`（实体键：projects/funds/approvals/capCalls/batches/distributions/reports/warnings/reminders/abandons/postEvents/todoView 配置与通用渲染器 `DM.renderList(pageKey)`）；数据从现有页面**逐页提取迁移，禁发明新业务数据**（新增行仅 T4 明确允许的 8 个未投归档项目）；
2. 接入页清单（写死 15 页）：F00（我的待办/我发起的/停滞提醒/业务速览计数）、F01（列表视图＋泳道）、F07、F10、F12、F13、F14、F15、F16、F17、F18（数据单源化为 dmFunnel 表＋口径注记，不做行级推导）、F19（放弃明细行与徽标计数由 abandons 推导）、F20（四视图）、F22、F23；保持静态不动：F02/F03/F08/F09/F11/F21（内容/配置型）与 F24；
3. 每页接入方式：`<head>` 加 `<script src="../_data/demo-data.js">`（同目录页无 `../`）＋页内小段列配置 script＋行区 tbody 改由 `DM.renderList` 初始渲染；**禁整文件重写**——用精确锚点替换，逐处断言；
4. **数据零丢失门**：每页迁移前快照 tbody 行数与首列文本集合，渲染后断言 DOM 行数一致且首列值集合一致；任一页不过 → 触发页面级降级条款（该页 git 回退静态，记失败清单，继续下一页），收尾报告 `接入 N/15`；
5. 弹窗预填（接入页内的新增/登记弹窗）从 store 取值，选项枚举不动。

### T2 · 页内动作真联动（因 1 可见化）

1. F20：审批处理弹窗「通过/退回/否决」提交 → 该行从「待我审批」视图迁移至对应视图＋四视图徽章计数实时更新（同一浏览器会话内内存态）；「撤回」同机制；
2. F01：行内「推进」（弹窗确认）→ 该行阶段徽章与状态文本更新；「放弃登记」确认 → 该行迁入未投归档态（列表内状态变化，不删行）；
3. F15：登记批次弹窗提交 → 批次表新增一行（数据入 store）；
4. 抽验页 F12：上传报告提交 → 报告表新增一行（状态「待归档」按现有枚举）；
5. 以上均须 Playwright 编程点击＋DOM 断言（先读后点后断言），其余接入页同机制但不逐页写死断言（抽验覆盖）。

### T3 · alert 查看类死端→真弹窗（因 2，与 T1 解耦可独立过门）

1. 分类口径（默认决策 1）：「查看/详情/预览/编辑」类 alert → 真弹窗；确认反馈类（已保存/已归档/已停用/已导出/已撤回/已推送等）**保留 alert**；改造前先产出全站 alert 分类清单（落 A17）；
2. 改造清单（按此优先级）：F20 审批单详情（最高优先：弹窗含审批编号/类型/关联业务/发起人/发起时间/当前节点/意见摘要，取值自该行 DOM 或 store）→ F19 未投归档详情 → F15 编辑批次（复用登记批次弹窗预填该行）→ F09 查看资质材料 → F08 文件预览 → F11 附件预览 → F12 报告预览 → F16 付款凭证 → F01 处置标注；
3. 弹窗复用各页现有 `.modal` CSS 骨架与标题样式；新增 id/函数一律 `dm-` 前缀；每处改动后该页 Playwright 必跑（JS 字符串教训）。

### T4 · 统计互溯补齐（因 3）

1. F01 补 8 个未投归档项目行（千乘出行/南汐生物/恒宇装备/晶彩光电/蓝湾储能/蓝湾机器人/青云网络/驭光微纳——放弃节点与末级原因对齐 F19 明细；单号按 PRJ-2025-00xx 顺延；行结构同构现有行），数据进 store 由渲染输出；
2. F19：放弃明细行与「GP·N 条/LP·N 条」徽标计数由 abandons 推导（推导值=行数，机器可断言）；
3. F18：漏斗区加灰字口径注记「累计口径＝全生命周期（含未投归档）；在管项目见项目库」，数字保持 dmFunnel 单源静态表。

### T5 · 演示话术落档（因 1/因 4 边界声明）

1. `P3-R02-演示场景覆盖梳理.md` 头部新增「演示开场声明与联动边界」节：单源联动说明（页内真联动/跨页一致性由单源保证/跨页动作传播为讲解词）、F24 角色下拉为演示选择器（角色权限看 F21）、分支场景话术（驳回重提/停滞处置/放弃回流不可）；
2. 不改既有 63 场景行的评级结论。

### T6 · 收尾落档（回写三件＋git 批次为硬性）

1. **A17**：新建 `P2-R01/P2-R01-A17-需求调整记录-20260914.md`（格式沿用 A15/A16）——alert 分类清单、接入页清单与接入结果 N/15、联动断言证据、默认决策命中、登记项（F24 角色真联动、跨页动作传播）；
2. **原型说明**：版本行升 **V1.10**（页面行不动；加 _data/demo-data.js 与联动/弹窗改造注记；接入未达 15/15 时如实记 N/15 与降级页）；PRD 10.2 加 A17 行；
3. `_索引.md` G03 行＋commit 哈希；`_AGENT基线.md` 滚动（V1.10/_data 单源/接入页清单/联动能力边界）；
4. git 两段提交（主体＋收尾回写）不 push，哈希回填本档头部与 `_索引.md`；
5. 失败清单结论行贴进对话（可为空清单，给「无失败项」）。

## 四、默认决策表（表外情形→最简合理默认＋收尾「默认决策命中」注明，全程禁止向用户提问）

1. **alert 分类**：标题/文案含 查看|详情|预览|编辑 → 改弹窗；其余一律保留；分类清单以改造前实测为准落 A17
2. **接入页集合**：T1 第 2 条写死的 15 页，不扩不缩；F02/F03/F08/F09/F11/F21/F24 零接入
3. **渲染架构**：`window.DM` 单全局＋`DM.renderList(pageKey)`＋页内列配置；不引入任何外部库/框架；数据文件纯 JS 对象字面量
4. **数据保真**：迁移只搬现有页面可见数据；新增行仅 T4 的 8 个未投归档项目（字段对齐 F19 明细＋行结构同构）
5. **联动持久化边界**：只做页内内存态联动；不做 localStorage/跨页持久（file:// 跨页会话不可靠，登记为已知边界）；演示重启即还原
6. **样式**：新弹窗/新徽章复用页面既有 class 体系；新命名一律 `dm-` 前缀；不改既有 id/class/JS 变量
7. **页面级降级**：接入页过不了数据零丢失门或 Playwright 报错 → git checkout 回退该页为静态、记失败清单、继续；收尾如实报告 `接入 N/15`
8. **执行序**：T1→T2→T3→T4→T5→T6；T3 与 T1 解耦（弹窗取值优先行内 DOM），T1 整体失败不阻塞 T3/T4（T4 的 F01 补行在回退静态时改为直接改静态行）
9. **版本行**：T6 时无论接入 N/15 一律升 V1.10（改动已发生）；接入数如实记录
10. **互斥与防覆盖**：mtime 互斥按前置校验 5；版本行高于 V1.9 → 停（P2-R01-A06 事故先例）；99-归档/`_p3_backup*`/A 系列历史记录不动
11. **工具**：一切校验用 Python（anaconda3 playwright-py）；本机无 node 禁依赖（demo-data.js 语法正确性由「Playwright 加载后 `window.DM` 存在＋0 JS 错误」断言替代 node --check）；中文路径 python 内嵌绝对路径；move 禁覆盖同名
12. **术语**：P1-R02 现行词；C 类业务口径词永不改；带圈序号 ①~⑫ 现状保留（P2 待议 1）
13. **git**：两段提交不 push；提交失败记失败清单不阻塞判定；哈希回填两处
14. **标注层**：D02 json 仅当本 goal 改动文本命中 selector/标注文本才同步＋重注入零告警，否则零改动

## 五、失败处理策略（无人值守铁律）

任何 assert 失败/计数不符/预期外结构：跳过该项 → 记失败清单（时间/任务/对象/原因/处置）→ 继续下一项；接入页失败走默认决策 7 的页面级降级。禁止卡住等待、禁止对不确定结构猜测性改写、禁止静默吞掉。收尾报告必须含失败清单结论行（可为空）。

## 六、纪律（违反=返工）

- `_AGENT基线.md` 第六节 逐条适用（术语唯一源/不动项/防并行覆盖/落档/工程纪律）
- 按任务动作类型反查教训全集：**JS/HTML 结构改动类**（精确锚点替换+逐处计数断言、标签配平自检、改 JS 后该页 Playwright 必跑防引号丢失、业务页面禁整文件重写）；**Playwright 验证类**（file:// 用 pathlib.as_uri、每页新建 page、双监听 pageerror+console.error 防监听器累积、编程点击后等待再断言）；**文件移动类**（python shutil.move 内嵌中文绝对路径、目标同名跳过留档）；**标注层类**（改标注只改 json 重跑注入、注入器以子文件夹为 --pages 根）
- T1 大改前对 21 页打 git 存档提交（降级回退的基准）；改造全程同一工作区单写者

## 七、验证门（全部满足才算完成，证据形态逐条点名，输出贴进对话）

1. **前置校验**：六项逐条一行输出（版本行实测值/失败清单不存在/_data 不存在/索引无 G03 完成态/mtime 差值/存档提交号）
2. **T1 数据零丢失**：每接入页一行 `数据零丢失 <页名>: 行数 前=X 后=X；首列集合一致=真`；末行 `接入 N/15（降级页：…）`；Playwright 加载后 `window.DM 存在=真` 且接入页 0 pageerror/0 console.error
3. **T2 联动断言**：Playwright 逐条 PASS 行——F20 通过一条→待审徽章-1 且行迁移；F01 推进→行内阶段徽章变化；F15 登记批次→行数+1；F12 上传报告→行数+1
4. **T3 死端清零**：python 全站断言一行 `查看/详情/预览/编辑类 alert=0（改造前 N 处）`；F20 审批单详情弹窗 Playwright 打开断言 PASS 行；确认反馈类 alert 保留数=清单数（一行）
5. **T4 互溯**：一行 `F19 放弃项目在 F01 命中=10/10`；F18 口径注记存在=真；F19 徽标计数=abandons 行数（一行断言）
6. **全量回归**：22 页逐页 `PASS <文件名>` 清单＋末行 `PASS 22/22, FAIL 0, JS错误 0`
7. **落档**：grep 断言四行——`A17 存在且含默认决策命中：真`／`原型说明版本行含 V1.10：真`／`PRD 10.2 含 A17 行：真`／`_索引 含 G03 行且含 commit 哈希：真`
8. **失败清单**：结论行原文贴出（空清单给「无失败项」）
9. **git**：两段提交哈希两行输出

## 八、执行记录（执行 agent 填写）

- 状态：待执行
- 默认决策命中：（收尾填写）
- 失败清单结论：（收尾填写）
- commit：（收尾填写两段哈希）
