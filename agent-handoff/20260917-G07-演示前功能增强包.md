# 20260917-G07-演示前功能增强包

> **状态**：待执行
> **建档**：2026-09-17（用户指令：G04 调研借鉴落地——功能层面五项低成本高收益增强打包执行；「F20 回写预览」性价比最高）
> **项目根（全部相对路径以此拼接，cwd 不可依赖）**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow`
> **失败清单（绝对路径）**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/goal-20260917-演示前功能增强包-失败清单.md`
> **形态**：/goal 条件路由（A 形态）——一切判定口径/验收锚点/默认决策以本文档为准；本文档=该 goal 的完整凭据与执行记录处
> **上下文基线**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/_AGENT基线.md`（先读全文再动手）
> **任务性质**：**原型改造类**（G03/G06 同款模式：demo-data.js 数据行＋页内小改）——与 G04 调研类相反，允许写入原型域，但**仅限第六节允许清单**
> **上游依据**：《P2-R01-D05-国内SaaS功能差异分析报告》（G04 产出，commit 2174729）——五项增强的竞品信源均已挂 D05，本档不重复引 URL

---

## 一、背景与目标

G04 调研（D05）结论：demo 业务设计层领先（特色 3 项），差距集中在工程深度（缺失 9/深度差距 4）。其中五项可按 G03/G06 既有模式（demo-data.js 数据行＋页内 dm- 函数/徽标/说明行）在**原型演示层**落地，把竞品的机制叙事演出来，演示时从「口头解释」升级为「指着讲」：

| 项 | 增强点 | 竞品叙事（D05 信源编号） | 落点页面 |
|---|---|---|---|
| E1 | 审批「回写预览」 | 审批通过自动回写业务对象=行业标准机制（L2：金蝶下推/反审核、明道云节点通过后更新） | F20 |
| E2 | 逾期三级升级可视化 | 蓝凌 notify/limit 分离、明道云超时策略（L2 §2.1/§2.3） | F20 |
| E3 | 分配通知环节 | Carta 分配四步：计算→通知→付款→入账（L2 §2.5） | F16 |
| E4 | 缴款通知书静态行 | eFront 认缴实缴自动计算生成通知书（L2 §2.5） | F08 |
| E5 | 预警阈值触发口径 | 卓沃「监控阀值（可自由设置）」＋PEPM 农发四类自动预警（W3/L3） | F13 |

**目标终态**：E1~E5 全部落地＋23 页 Playwright 回归全绿＋A23 落档＋回写三件；原型说明 V1.14→V1.15。

## 二、前置校验（先于一切；任一不符 → 写失败清单〔路径见头部〕注明后收尾，禁止重做）

| # | 标记（项目根相对） | 期望值 |
|---|---|---|
| 1 | `P3-R01-原型说明.md` 版本行 | 含 `V1.14`（V1.15+ = 停，先回读现状修订本档；版本行尾含「09-14 G04 调研类产出（P2-R01-D05），原型零改动」注记属预期，勿误判） |
| 2 | `agent-handoff/goal-20260917-演示前功能增强包-失败清单.md` | 不存在（校验过即创建，表头：时间/任务/对象/原因/处置） |
| 3 | `P2-R01/P2-R01-D05-国内SaaS功能差异分析报告.md` | **存在**（上游依据在位；缺失=停，D05 未落档不可凭空执行） |
| 4 | `agent-handoff/_索引.md` | 无状态为「已完成」或含 commit 哈希的 `| G07 |` 行（预登记待执行行不算） |
| 5 | `P3-R01-原型/` 与 `agent-handoff/` 最近文件 mtime | ≥30 分钟（本档、goal条件文档、G04 两文档、失败清单豁免）；不符=并发 → 停 |
| 6 | git status | 有未提交改动 → 先 `git add -A && git commit -m "goal 前工作区存档"`（不 push）再动手 |

## 三、任务组 T1~T4（执行序=编号序）

### T1 · 盘点定稿（禁凭记忆改）

1. 读 `P3-R01-原型/_data/demo-data.js`：approvals（APV-2026-0025~0040、apType 字段）/batches/distributions/funds/warnings/reminders 各段现结构；
2. 读五目标页现有弹窗与行结构：F20 处理弹窗（通过/退回/否决三动作，alert 文案「已通过审批：业务阶段与状态将自动更新…」）、F14 规则行与处理 alert、F16 分配单行（DST-2026-001）与「登记付款」动作、F08 出资记录页签与 dmFileView 真弹窗先例、F13 处理说明留痕弹窗（G06）与示例行；
3. 在执行记录或临时笔记确定每项插入锚点（精确串），**禁止整文件重写，插入用精确串定位＋替换前后计数断言**。

### T2 · 五项增强 E1~E5（按序执行，每项完成即页内 file:// 自检）

**E1 · F20 审批回写预览（性价比最高，若时点紧可仅做本项后直接跳 T3/T4 并在失败清单注明其余项跳过）**
- 处理弹窗「通过」路径的反馈升级为含**回写效果说明行**：新增页内函数 `dmG07Writeback(apNo)`（dmG07 前缀），弹真弹窗（复用 G06 F13 处理弹窗同款样式），内容=按该单 apType 映射的回写说明。映射表写死（7 类全覆盖）：
  - 立项审批 → 「项目阶段⑤立项→⑥投资人路演；④尽调资料包自动锁定归档；生成『⑥ 投资人路演』待办」
  - 投决上会 → 「项目阶段⑥→⑦投决结论落档；投决包自动锁定归档；通过则生成基金设立待办」
  - 基金注册审批 → 「项目阶段⑦→⑧基金设立；基金状态置『设立中』；LPA/工商文件包锁定归档」
  - 付款审批 → 「项目阶段⑧→⑨出资；实缴记录生成；付款凭证包锁定归档」
  - 收益分配审批 → 「分配单生效；批次状态置『已完成』；分配通知待推送」
  - 注销审批 → 「基金状态置『已注销』；清算材料包锁定归档；项目档案转终态」
  - 认定审批 → 「LP 认定状态置『已认定』；认定有效期 24 个月自通过日起算；认定材料归档」
- 说明行文案前缀统一「通过后自动回写：」。

**E2 · F20 逾期三级升级徽标**
- 「待我审批」视图选一条现有待办单（T1 盘点时定，优先 APV-2026-0030 类非新造），行内加小标「⚠ 逾期 15 天 · 已升级推送上级」（A21 口径 7 提醒→15 升级→30 再升级的中间档演示）；点击或随处理弹窗展示一句说明「逾期三级：7 天提醒→15 天升级推送上级→30 天再升级（A21 规则 1）」。
- 禁改 slaHours 字典、禁新增审批单数据行（复用现有单）。

**E3 · F16 分配通知环节**
- 分配单列表加「通知状态」列（存量行 DST-2026-001 置「已通知 LP」tag-green）＋行内动作「推送分配通知」→ 确认类 alert「分配通知已推送至 LP 联系人」（确认反馈类 alert 按 G03 口径可保留）。
- 列结构变更须同步表头列数与 DM.pages 配置行模板（F16 为 G03 接入页，改列必改配置并回归）。

**E4 · F08 缴款通知书静态行**
- 出资记录页签文件区新增一行「缴款通知书-FUND-2025-011-001.pdf」（静态演示数据），点击走 `dmG07NoticeView()` 真弹窗（样式复用 dmFileView 先例），弹窗文案「缴款通知书（演示态）：本次实缴金额、缴款账户与截止日期——BS 版由系统按认缴计划自动生成」。
- 禁做真实 PDF、禁动既有文件行。

**E5 · F13 预警阈值触发口径**
- 处理说明留痕弹窗（G06 已有）增「触发口径」行：示例写死「收盘价 0.82 × 成本价，触及预警线（阈值 0.8）」（股价类）；舆情类示例行弹窗显示「触发口径：监听关键词命中（阈值：企业名+负面词库）」。
- 至少一行示例数据文案带上阈值语义；禁改 F13 列结构与来源枚举。

### T3 · 回归验证

1. Playwright file:// 直开（pathlib.as_uri）23 页全量：每页新建 page、双监听 pageerror+console.error、防监听器累积；23 页全绿清单 23 行输出；
2. 数据层断言：任取一接入页加载后 `window.DM.store.approvals` 与 `dictionaries` 段存在、长度>0；
3. 新增交互抽验：F20 处理弹窗打开→回写预览可见；F16 推送分配通知动作可点；F08 通知书行弹窗可开；F13 处理弹窗含触发口径行（Playwright 点击断言或 DOM 存在断言）。

### T4 · 收尾落档（回写三件＋git 批次为硬性）

1. **A 记录**：新建 `P2-R01/P2-R01-A23-需求调整记录-20260917.md`（A22 已被 G04 占用，A23=最小空号；格式沿用 A16/A22；内容=五项增强执行摘要/验收断言结果/默认决策命中）；
2. **原型说明**：版本行 V1.14→**V1.15**（重写版本行，写明 G07 五项增强；保留 G04 调研注记语义），页面清单相应页注记；
3. **PRD 10.2** 加 A23 一行；**P3-R02** 开场声明补一条「G07 增强元素口径」（回写预览=演示说明卡/缴款通知书=静态演示态），S-17~24、S-46、S-51/52 相关场景行动线补注（最小改动）；
4. **回写三件**：本档标完成＋执行记录（默认决策命中/失败清单结论/两段哈希）；`_索引.md` G07 行已完成＋主体哈希；`_AGENT基线.md` 滚动（版本 V1.15、五项增强一句、D05 呼应注——「D05 缺失/深度差距清单中 E1~E5 对应项已落演示态，仍属深度差距非产品级」）；
5. git 两段提交（主体增强＋收尾回写）不 push，哈希回填本档头部与 `_索引.md`。

## 四、默认决策表（表外情形→最简合理默认＋收尾「默认决策命中」注明，全程禁止向用户提问）

1. **增强范围**：仅 E1~E5；二档项（F00 驾驶舱占位卡/全局搜索框）与三档项（回收站/LP 门户/流程引擎自定义/移动端）**不纳入**——回收站与「未投归档不可回流」终态矛盾，其余产品级演不了
2. **A 取号**：执行时列 `P2-R01/P2-R01-A*.md` 取最小空号（当前下一空号 **A23**；若又被占则顺延并在执行记录写死）
3. **函数/id 前缀**：一律 `dmG07`（dmG07Writeback/dmG07NoticeView 等），禁改既有 id/class/JS 变量；G03 口径「新增 dm- 前缀」的延伸
4. **弹窗形态**：查看/说明类一律真弹窗（复用 G06 F13 处理弹窗样式）；「推送分配通知」为确认反馈类 alert 可保留；**禁新增查看类 alert**
5. **数据纪律**：E2/E4/E5 复用现有数据行或纯静态文案，不新增 approvals/warnings 主数据行（E3 通知状态列挂现有 DST-2026-001）；demo-data.js 改动仅限 E3 列配置所需最小段
6. **E2 演示单**：优先 APV-2026-0030（待我审批视图现有行）；若该单结构不宜挂标，换任一「待我审批」现存单并在执行记录注明
7. **版本号**：V1.14→V1.15 一档（五项小改不升大档）；版本行保留 G04 注记原文拼接
8. **D02 标注 json/D04 导航图**：不动、不重注入（新增元素不要求标注）
9. **P3-R02 修改**：仅开场声明补一条＋既有场景行动线补注，禁整节重写
10. **D05 报告**：不改（调研快照性质）；呼应关系写进 A23 与基线
11. **术语**：P1-R02 现行词；C 类业务口径词永不改；新文案风格对齐 admin-ui-spec 与既有弹窗文案
12. **git**：两段提交不 push；提交失败记失败清单不阻塞判定；哈希回填两处

## 五、失败处理策略（无人值守铁律）

任何 assert 失败/锚点不符/预期外结构：跳过该项 → 记失败清单（时间/任务/对象/原因/处置）→ 继续下一项。E1~E5 任一项失败不阻塞其余项与收尾。禁止卡住等待、禁止猜测性改写页面结构、禁止静默吞掉。收尾必须含失败清单结论行（可为空给「无失败项」）。

## 六、原型域写入允许清单（越界=违规，验证门 5 断言）

| 允许 | 说明 |
|---|---|
| `P3-R01-原型/审批中心/P3-R01-F20-审批中心.html` | E1/E2 |
| `P3-R01-原型/投后管理/P3-R01-F14-提醒中心.html` | E2 备选落点（若 E2 落 F20 则本文件零改动） |
| `P3-R01-原型/退出管理/P3-R01-F16-收益分配.html` | E3 |
| `P3-R01-原型/基金管理/P3-R01-F08-基金详情.html` | E4 |
| `P3-R01-原型/投后管理/P3-R01-F13-风险预警.html` | E5 |
| `P3-R01-原型/_data/demo-data.js` | 仅 E3 列配置所需最小改动 |
| 根层：原型说明/PRD/P3-R02/P2-R01-A23/agent-handoff 三件 | 落档回写 |

**禁改**：其余 18 页、D02-原型标注数据.json、P3-R01-D04、99-归档/、`_p3_backup*`、A 系列历史记录。

## 七、验证门（全部满足才算完成，证据形态逐条点名，输出贴进对话）

1. **前置校验**：六项逐条一行输出（版本行实测值/失败清单不存在/D05 存在/索引无 G07 完成态/mtime 差值/存档提交号）
2. **五项增强 grep 断言五行**（python，关键词写死）：
   - E1：F20 含「通过后自动回写」≥1 处且含 `dmG07Writeback` ≥1 处
   - E2：F20（或 F14，以实际落点为准）含「已升级推送上级」≥1 处
   - E3：F16 含「分配通知」≥2 处且含「已通知 LP」≥1 处
   - E4：F08 含「缴款通知书」≥1 处且含 `dmG07NoticeView` ≥1 处
   - E5：F13 含「预警线」或「触发口径」≥2 处
3. **回归**：Playwright 23 页 PASS 清单 23 行（页名+PASS）＋`window.DM.store.approvals` 存在断言一行
4. **改动域断言**：`git diff --name-only <存档提交>..HEAD` 逐行核对全部落在第六节允许清单（一行结论＋清单输出）
5. **落档 grep 四行**：A23 存在且含默认决策命中：真／原型说明版本行含 V1.15 与 G07 注记：真／PRD 10.2 含 A23 行：真／_索引 G07 行含 commit 哈希：真
6. **失败清单**：结论行原文贴出（空清单给「无失败项」）
7. **git**：两段提交哈希两行输出

## 八、执行记录（执行 agent 填写）

- 状态：待执行
- 默认决策命中：（收尾填写）
- 失败清单结论：（收尾填写）
- commit：（收尾填写两段哈希）
