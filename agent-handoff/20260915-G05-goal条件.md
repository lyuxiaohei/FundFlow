/goal 执行「数据字典与全量数据驱动」G05：基础数据更名数据字典（F25 文件 git mv＋21 侧栏＋权限树文案＋D02 键）＋demo-data.js 新增 dictionaries 单源（7 既有分类逐字迁移＋9 新分类收纳=16）＋F25 字典页 DM 渲染＋11 页枚举下拉 dmFillSelect 数据驱动（options 零丢失门）＋话术与 A20 落档＋独立子agent 复核，全部完成后回写台账。工作目录（下列交接文档全部相对路径之根，不依赖会话 cwd）：/Users/bailey/Desktop/xiaohei-workplace/FundFlow

【交接文档 · 双层路由，先读全文再动手】
/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/_AGENT基线.md（项目上下文与铁律，常驻滚动）
/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/20260915-G05-数据字典与全量数据驱动.md（本 goal 任务全量细则：任务组 T1~T7／16 分类与 12 位点清单／默认决策表 16 条／页面级降级条款／验证门与子agent复核模板）

【前置校验 · 先于一切；任一不符 → 写失败清单（路径在任务文档头部）后收尾，禁止重做】
1. P3-R01-原型说明.md 版本行含 V1.12（V1.13+ = 疑似已执行/并发 → 停）
2. agent-handoff/goal-20260915-数据字典-失败清单.md 不存在
3. _data/demo-data.js 存在 且 store.dictionaries 不存在（存在=疑似已执行 → 停）
4. agent-handoff/_索引.md 无「已完成」或含 commit 哈希的「| G05 |」行（预登记待执行行不算）
5. P3-R01-原型/ 与 agent-handoff/ 最近文件 mtime ≥30 分钟（本档、失败清单、G05 任务文档与 goal条件文档豁免）
6. git status 有未提交改动 → 先 git add -A && git commit 存档（不 push）再动手（该存档号同时=T7 复核基线）

【任务 · 唯一口径=任务文档】
T1 更名：F25 git mv 数据字典＋21 页侧栏/F21 权限树文案/D02 键随迁＋旧名引用清零
T2 字典单源化：dictionaries 段（数据自 F25 静态逐字提取）＋F25 左导航/面板/徽标 DM 渲染＋逐分类零丢失门＋新增数据项提交行联动
T3 枚举收纳：9 新分类（审批6/退出3/事件5/专项报告3/基金文件5/组织形式2/预警2/信息来源3/角色4）＋盘点「不入」边界表落 A20
T4 下拉驱动：12 位点 dmFillSelect（F01 行业10/轮次7＋GP/LP 两级原因、F02、F07=2、F08=2+6、F11=5、F12=3、F14=5、F15=3、F20=7、F21=5、F24=4），options 前后序列一致断言；失败页 git 回退留档（降级条款）
T5 话术文档：P3-R02 边界节补行＋术语表词条（A10 翻转注记）
T6 收尾：A20＋V1.13＋PRD 10.2＋D03＋_索引＋基线＋两段提交哈希回填
T7 独立复核：按任务文档 T7 模板派零上下文子agent 重跑验证门 1~9（禁采信执行自测；「前」基线自存档提交号 git show 重取；只读），报告全文原样贴对话；任一 FAIL→修复后复跑一次，仍 FAIL→失败清单如实收尾，禁止三跑

【完成判定 · 双轨=执行自测＋独立子agent复核，子agent总结论 PASS 才算完成；证据全部贴进对话，评估器只认对话内内容】
1. 前置校验六项逐条一行输出
2. T1：python 一行「全站旧名引用=0；侧栏数据字典=21 页 × 1」
3. T2：7 分类逐行「行数 前=X 后=X；首列集合一致=真」＋16 分类渲染 PASS＋window.DM=真＋0 JS 错误＋新增项行数+1 PASS
4. T3：一行「字典分类=16（7+9）；新分类行数=6/3/5/3/5/2/2/3/4」＋A20 含盘点边界表
5. T4：12 位点逐行「options 前后序列一致=真」＋末行「驱动 12/12（降级页：…）」
6. 全量回归：逐页 PASS 清单＋末行「PASS 23/23, FAIL 0, JS错误 0」
7. 落档 grep 四行（A20 含默认决策命中／版本行 V1.13／PRD 10.2 含 A20／_索引 G05 行含哈希）
8. 失败清单结论行原文（空清单给「无失败项」）
9. git 两段提交哈希两行
10. 子agent复核报告全文（复核1~9 逐项 PASS/FAIL＋末行「子agent复核总结论：PASS（9/9）」）——本条为最终裁决；自测输出仅过程证据

【约束】
- 无人值守：不需要也不得向用户提问；决策按任务文档默认决策表 16 条；拿不准按最简合理默认执行并在收尾「默认决策命中」节注明
- assert 失败/计数不符 → 跳过该项 → 记失败清单 → 继续；T4 页失败走页面级降级（回退静态留档），禁止带病上线
- 术语：P1-R02 现行词，C 类业务口径词永不改；既有 HTML id/class/JS 变量不动（新增一律 dm- 前缀）；A10 历史层原词不动；99-归档／_p3_backup*／A 系列历史记录不动
- 工具：本机无 node，一切校验用 Python（anaconda3 playwright-py）；中文路径 python 内嵌绝对路径；move 禁覆盖同名；业务页面禁整文件重写（精确锚点＋锚定同元素断言——禁未锚定跨元素正则，A18 事故教训）；改 JS 后该页 Playwright 必跑
- 子agent 纪律：T7 复核用全新会话派发、只读、禁采信执行输出；复核临时产物限 agent-handoff/_g05_verify/ 不入提交
- git 批次（硬性）：T1 前先打存档提交；两段提交（主体＋收尾回写）不 push，哈希回填任务文档与 _索引.md
- 收尾回写三件（硬性）：任务文档标完成＋执行记录（含子agent复核结论）；_索引.md 挂 G05 行与哈希；_AGENT基线.md 同步新现状

or stop after 150 turns