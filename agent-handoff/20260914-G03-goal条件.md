/goal 执行「演示闭环修复」G03：_data/demo-data.js 单源数据层 15 页接入＋页内动作真联动＋alert 查看类死端改真弹窗＋统计互溯补齐＋演示话术落档，全部落档后回写台账。工作目录（下列交接文档全部相对路径之根，不依赖会话 cwd）：/Users/bailey/Desktop/xiaohei-workplace/FundFlow

【交接文档 · 双层路由，先读全文再动手】
/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/_AGENT基线.md（项目上下文与铁律，常驻滚动）
/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/20260914-G03-演示闭环修复.md（本 goal 任务全量细则：任务组 T1~T6／默认决策表／页面级降级条款／验证门）

【前置校验 · 先于一切；任一不符 → 写失败清单（路径在任务文档头部）后收尾，禁止重做】
1. P3-R01-原型说明.md 版本行含 V1.9（V1.10+ = 疑似已执行/并发 → 停）
2. agent-handoff/goal-20260914-演示闭环-失败清单.md 不存在
3. P3-R01-原型/_data/demo-data.js 不存在（存在=疑似已执行 → 停）
4. agent-handoff/_索引.md 无状态为已完成或含 commit 哈希的「| G03 |」行（预登记待执行行不算）
5. P3-R01-原型/ 与 agent-handoff/ 最近文件 mtime ≥30 分钟（本档、失败清单、G03 任务文档与 goal条件豁免）
6. git status 有未提交改动 → 先 git add -A && git commit 存档（不 push）再动手

【任务 · 唯一口径=任务文档】
T1 新建 P3-R01-原型/_data/demo-data.js 单源数据层＋15 页接入（数据零丢失门，页面级降级条款）
T2 页内动作真联动：F20 审批三态迁移＋徽章、F01 推进/放弃行内变化、F15/F12 提交新增行（Playwright 点击断言）
T3 alert 查看类死端→真弹窗（分类清单落 A17；确认反馈类保留；F20 审批单详情最优先）
T4 统计互溯：F01 补 8 个未投归档项目行＋F19 计数由单源推导＋F18 口径注记
T5 P3-R02 头部加「演示开场声明与联动边界」节
T6 收尾落档与回写三件

【完成判定 · 证据全部贴进对话，评估器只认对话内内容】
1. T1：每接入页「数据零丢失」断言行＋末行「接入 N/15」＋window.DM 存在=真＋接入页 0 JS 错误
2. T2：Playwright 逐条 PASS 行（F20 徽章-1 且行迁移／F01 阶段徽章变化／F15 行数+1／F12 行数+1）
3. T3：python 全站断言一行「查看/详情/预览/编辑类 alert=0（改造前 N 处）」＋F20 详情弹窗打开 PASS 行
4. T4：一行「F19 放弃项目在 F01 命中=10/10」＋F18 注记存在=真＋F19 徽标=abandons 行数
5. 全量回归：逐页 PASS 清单＋末行「PASS 22/22, FAIL 0, JS错误 0」
6. 落档：grep 四行（A17 存在含默认决策命中／版本行 V1.10／PRD 10.2 含 A17 行／_索引 G03 行含 commit 哈希）
7. 失败清单结论行原文（空清单给「无失败项」）

【约束】
- 无人值守：不需要也不得向用户提问；决策按任务文档默认决策表；拿不准按最简合理默认执行并在收尾「默认决策命中」节注明
- assert 失败/计数不符 → 跳过该项 → 记失败清单 → 继续；接入页失败走页面级降级（回退静态留档），禁止带病上线
- 术语：P1-R02 现行词，C 类业务口径词永不改；既有 HTML id/class/JS 变量不动（新增一律 dm- 前缀）；D02 标注 json 命中才同步重注入；99-归档／_p3_backup*／A 系列历史记录不动
- 工具：本机无 node，一切校验用 Python（anaconda3 playwright-py；demo-data.js 语法以 window.DM 加载断言替代 node --check）；中文路径 python 内嵌绝对路径；move 禁覆盖同名；业务页面禁整文件重写（精确锚点+断言）
- git 批次（硬性）：T1 大改前先打 git 存档提交；两段提交（主体＋收尾回写）不 push，哈希回填任务文档与 _索引.md
- 收尾回写三件（硬性）：任务文档标完成＋执行记录；_索引.md 挂 G03 行与哈希；_AGENT基线.md 同步新现状

or stop after 150 turns
