# 20260912-G02-原型演示前完善

> **状态**：待执行
> **建档**：2026-09-12（对比汽车物流包装租赁项目会前打磨先例后立项，见「背景」）
> **项目根（全部相对路径以此拼接，cwd 不可依赖）**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow`
> **失败清单（绝对路径）**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/goal-20260912-演示前完善-失败清单.md`
> **形态**：/goal 条件路由（A 形态）——一切判定口径/计数预期/默认决策以本文档为准；本文档=该 goal 的完整凭据与执行记录处
> **上下文基线**：`/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/_AGENT基线.md`（先读全文再动手）
> **前置依赖**：无强制（G01 未执行则由 T0 顺带执行）
> **硬时限语境**：用户数日内要做原型演示——本 goal 一次性跑完，不留尾巴（登记项除外）；不等待、不分批

---

## 一、背景与目标（为什么做这几件事）

对照包装租赁项目（`/Users/bailey/Desktop/xiaohei-workplace/auto-parts-rental`）已实战验证过的会前打磨体系，本项目（FundFlow）演示前有四类缺口：

1. **词面（P0）**：G01 命名合规治理待执行——F21 约 70 处「字典」族开发词、F22/F23「按段切片」黑话、F20「我的待审」等，演示时被客户看见即减分；
2. **卫生（P0）**：`P3-R01-原型/` 根层残留 `_demo_*.html` 六个历史脚手架文件，演示现场误开风险；
3. **检验（P0）**：A01~A12 十一份需求调整记录**从未系统核查落地情况**；二十一个页面**没有一份"演示场景→页面→示例数据"覆盖清单**（包装租赁先例：P3-R04 梳理 76 场景→G24 只读检查→G25 修复包，三步全过）；
4. **观感（P1）**：包装租赁有 PC 登录页+默认登录态（G19b）、四态文案、移动端 H5；本项目均无——其中登录页增量低风险可补，其余登记不执行。

**目标终态**：G01 收口＋原型卫生清理＋《演示场景覆盖梳理》与《决策落实与场景闭环检查报告》两份新文档落地＋其中 P0 小补项修复完毕＋21 页 Playwright 全绿，演示可直接按覆盖梳理文档走场。

## 二、前置校验（先于一切；任一不符 → 写失败清单〔路径见头部〕注明后收尾，禁止重做）

| # | 标记（项目根相对） | 期望值 |
|---|---|---|
| 1 | `P3-R01-原型说明.md` 版本行 | 含 V1.5 或 V1.6（V1.7+=疑似已执行/并发 → 停；为 V1.6 时须同时存在 `P2-R01/P2-R01-A13-*`，否则视为异常 → 停） |
| 2 | `agent-handoff/goal-20260912-演示前完善-失败清单.md` | 不存在（校验过即由本 goal 创建，表头：时间/任务/对象/原因/处置） |
| 3 | `P3-R02-演示场景覆盖梳理.md` 与 `P2-R01-D04-决策落实与场景闭环检查报告.md` | 均不存在（存在=疑似已执行 → 停） |
| 4 | `agent-handoff/_索引.md` | 无状态为「已完成」或含 commit 哈希的 `| G02 |` 行（20260912 建档预登记的「待执行」行不算已执行；追记 2026-09-12 执行会话） |
| 5 | `P3-R01-原型/` 与 `agent-handoff/` 最近文件 mtime | ≥30 分钟（豁免：本档、失败清单、20260912 建档五件〔本档/_索引/_AGENT基线/G01/goal条件〕）；不符=并发 → 停（追记 2026-09-12 执行会话） |
| 6 | git status | 有未提交改动 → 先 `git add -A && git commit -m "goal 前工作区存档"`（不 push）再动手，保证每步可回溯 |

## 三、任务组 T0~T6（执行序=编号序；会前优先级已内含）

### T0 · 执行 G01 命名合规治理（词面 P0）

1. 按 `/Users/bailey/Desktop/xiaohei-workplace/FundFlow/agent-handoff/20260908-G01-命名合规治理.md` 全量执行 T1~T9（含其前置校验、默认决策表、验证门），本档**不重复规格**；
2. G01 的前置校验 1（版本行含 V1.5）：若实测已是 V1.6 且 A13 存在 → 判定 G01 已执行，T0 整体跳过并在执行记录留档一行「G01 已执行，跳过」；
3. G01 验证门七项证据全部贴进对话；其失败清单允许非空（跳过-留档-继续属正常完成态）。

### T1 · 原型卫生：`_demo_*.html` 六件出清（P0，防误开）

1. 先断言 21 F 页+D04 导航图内**无任何 `_demo_` 引用**（grep 计数=0）；有引用 → 跳过本任务记失败清单，禁移动；
2. python shutil.move 六件移至 `99-归档/P3-R01-demo脚手架-20260912/`（目标同名已存在 → 跳过该件记失败清单，禁覆盖）；
3. 不改这六个文件内容（纯移动）。

### T2 · 产出《P3-R02-演示场景覆盖梳理.md》（检验 P0·场景基准，先于 T3）

1. 置项目根 `/Users/bailey/Desktop/xiaohei-workplace/FundFlow/P3-R02-演示场景覆盖梳理.md`；对标物=包装租赁 `P3-R04-演示场景覆盖梳理.md`（路径见基线四）；可参照 99-归档/P3-R01-D03-演示导览-业务流程.html 的旧走场结构；
2. 场景全集按业务线分节：项目管道十二阶段①~⑦、放弃与未投归档支线、六节点在线审批、基金设立与出资⑧⑨、LP 认缴实缴、投后⑩⑪、退出三段⑫与收益分配/清算注销、统计报表、工作台与权限——九节覆盖 21 页与 D04 主线；
3. 每场景一行、格式写死：`| S-两位序号 | 业务场景（平时怎么干） | 演示动线（页→单→弹窗） | 覆盖 | 现状与缺口 | 修复动作 | 优先级 |`，编号从 S-01 连续递增；
4. 覆盖评级三值：✅=页面+示例行+数据互溯一致可指着讲；⚠️=有页缺行/缺字段/缺联动（小补可修）；❌=缺页面/需开发性补齐/需业务拍板（只登记）；
5. 优先级三值：P0=必演主线；P1=备用；P2=可缓。文档头部含目的、口径来源（PRD/D03-功能菜单与字段清单/A09/A12/D04）、图例、✅/⚠️/❌ 汇总行；
6. 口径来源逐页核对手册：每条动线须在对应 F 页内实测存在该入口/示例行（python 读文件断言，禁凭记忆写「有」）。

### T3 · 产出《P2-R01-D04-决策落实与场景闭环检查报告.md》（检验 P0·只读检查，零原型写入）

1. 置 `/Users/bailey/Desktop/xiaohei-workplace/FundFlow/P2-R01/P2-R01-D04-决策落实与场景闭环检查报告.md`；**本任务组不改任何原型文件**；
2. §一 决策落实核查：P2-R01/ 现存 A 系列记录逐份核查（实测 11 份，A03 空号）——每份 ≥1 个决策点 × 原型证据文件 × 落实判定；判定枚举写死：`已落实`/`部分落实`/`未落实`/`待议项（A13 已登记）`，每行附证据形态（页内文本断言/grep 命中行）；
3. §二 场景闭环核查：以 T2 产出的 P3-R02 为场景基准，汇总其中全部 ⚠️/❌ 项；
4. §三 修复建议：每条建议一行 `R-两位序号 | 描述 | 涉及文件 | 修复形态 | 优先级`，修复形态枚举写死：`小补`（补示例行/补字段/改可见文案，页面与数据层内可完成）/`开发性`（新页面/新模块/JS 架构改动）/`待拍板`（需业务或用户决策）；R-01 起连续编号；
5. 报告头部含检查源清单（A 系列 11 份路径+P3-R02+基线）、判定枚举、汇总行（各判定/形态计数）。

### T4 · 执行会前修复包（检验 P0·收口，白名单制）

1. 范围=T3 报告 §三 中**修复形态=小补 且 优先级=P0** 的全部 R 项；按 R 序号顺序执行；无可执行项 → 空跑留档「无可执行小补项」即过；
2. **白名单**：允许改动面=`P3-R01-原型/` 下 21 F 页与 `P3-R01-D02-原型标注数据.json`，且改动仅限三类——补示例数据行/补字段/改可见文案；白名单外零改动；
3. 新增示例行规范：沿用该页现有行 HTML 结构同构（先读一条现有行再仿写）；单号/日期/金额格式以 `P2-R01-D03-功能菜单与字段清单.md` 为准；金额=阿拉伯数字+中文大写并存（F10 反转后口径）；日期用 2026-09 内；
4. 每项执行前先红（断言现状确缺）后绿（改后断言通过），两段输出均留档；标注 json 命中改动则重跑注入且零告警；
5. 动手前 `git status --porcelain` 拍快照，收口后差集 ⊆ 白名单（机器断言）；JS 字符串有改动 → 该页 Playwright 必跑。

### T5 · 新建登录页 F24（观感 P1·可降级）

1. **降级条款**：T0~T4 存在未决失败项或未全部过门 → 本任务整体降级，仅在执行记录与 A14 登记一行，不阻塞收尾；
2. 新建 `/Users/bailey/Desktop/xiaohei-workplace/FundFlow/P3-R01-原型/工作台/P3-R01-F24-登录.html`：admin-ui-spec 体系（#1677ff、复用 F00 的 CSS 骨架风格）、纯 HTML 单文件、file:// 直开；含系统名、账号/密码输入（预填演示账号）、角色下拉（四角色名称与 F21 ROLE_PERMS 完全一致，先 grep F21 实测取值）、「登 录」按钮 go() 跳转 F00 工作台；
3. **不改动 21 个既有页面任何一行**；演示默认入口仍为 F00（登录页仅作演示开场可选项，开场方式由用户演示时自行选择，A14 注明）。

### T6 · 收尾落档（回写三件+git 批次为硬性）

1. **A14**：新建 `P2-R01/P2-R01-A14-需求调整记录-20260912.md`（格式沿用 A07~A12）——本 goal 全部改动+默认决策命中+登记项清单（T3 的开发性/待拍板 R 项、T5 降级与否、T1 移动记录）；
2. **原型说明**：T4 或 T5 有原型文件改动 → 版本行升 **V1.7**（改动页与新页 F24 行+本轮注记）；零改动 → 维持 V1.6 并在版本行补注「G02 检查类产出，原型零改动」；
3. **PRD 10.2**：加 A14、P3-R02、P2-R01-D04 三行（编码↔路径↔说明）；
4. `_索引.md`：G01、G02 两行补状态与 commit 哈希；`_AGENT基线.md` 三、现状基线滚动（页面数 21→22 若 F24 落地、版本、新文档、_demo_ 出清）；
5. git 两段提交：主体一段、收尾回写一段（`git add -A && git commit`，不 push），哈希回填本档头部与 `_索引.md`；
6. 失败清单结论行贴进对话（可为空清单，给「无失败项」）。

## 四、默认决策表（表外情形→最简合理默认+收尾「默认决策命中」节注明，全程禁止向用户提问）

1. **执行序**：T0→T1→T2→T3→T4→T5→T6；T2 先于 T3（T3 以 P3-R02 为场景基准）；T5 可降级；其余无先行依赖的项不得调序
2. **演示日期未定**：默认一次跑完不分批；「几天后」不写入文档为硬日期
3. **G01 口径**：完全按 G01 档执行，本档不重复其规格；两档冲突时以 G01 档为准（其专属域）
4. **P3-R02 场景数下界**：≥40 条（S-01 起连续编号）；少于 40=覆盖不足，继续补线不得收尾
5. **评级与形态枚举**：写死于任务组内三值/三值/三值集合，不得自创档位
6. **修复白名单**：仅 T4 第 2 条三类改动；任何 ❌/开发性/待拍板项一律登记不执行；「复核已合规无改动」记录不算失败
7. **示例行数据**：同构优先——与该页现有行字段逐一对齐；页面无同构行可仿 → 该项记失败清单跳过，禁发明新结构
8. **登录页**：文件名与路径写死（T5 第 2 条）；四角色取值以 F21 实测为准；不改既有页；跳转目标 F00
9. **版本行**：T0 后必然 V1.6；T6 时按 T4/T5 有无原型改动升 V1.7 或补注维持（判定=git diff 触及 P3-R01-原型/ 即「有改动」，机器可判）
10. **文档编码**：场景梳理=阶段级 R 续号 **P3-R02**（P1-R02-术语表先例）；检查报告=挂靠 PRD 的 D 续号 **P2-R01-D04**；登录页=挂靠 P3-R01 的 F 续号 **F24**（F04/F05/F06 已归档为空号，空号不复用，故取最大+1）
11. **互斥与覆盖**：mtime<30 分钟或版本行异常 → 停写失败清单（P2-R01-A06 并行覆盖事故先例）；99-归档/`_p3_backup*`/A 系列历史记录只进不动
12. **工具**：一切校验用 Python（anaconda3 playwright-py）；本机无 node，禁依赖；中文路径 python 内嵌绝对路径；move 禁覆盖同名；业务页面禁整文件重写
13. **git**：两段提交不 push；提交失败不阻塞完成判定但必须记失败清单；哈希回填两处（本档头部+_索引）
14. **术语**：新产出文档一律用 P1-R02 术语表现行词；C 类业务口径词永不改

## 五、失败处理策略（无人值守铁律）

任何 assert 失败/计数不符/预期外结构：跳过该项 → 记失败清单（时间/任务/对象/原因/处置）→ 继续下一项。禁止卡住等待、禁止对不确定结构猜测性改写、禁止静默吞掉。T5 触发降级条款按默认决策执行。收尾报告必须含失败清单结论行（可为空）。

## 六、纪律（违反=返工）

- `_AGENT基线.md` 第六节 逐条适用（术语唯一源/不动项/防并行覆盖/落档/工程纪律）
- 按任务动作类型反查教训全集：**文本替换类**（精确串+期望计数断言+幂等，业务页面禁整文件重写）；**文件移动类**（python shutil.move 内嵌中文绝对路径、move 禁覆盖同名、移动前后引用扫描）；**JS 字符串类**（改后该页 Playwright 必跑，防引号丢失语法坏整页功能挂）；**Playwright 验证类**（file:// 用 pathlib.as_uri、每页新建 page、双监听 pageerror+console.error 防监听器累积、中文 URL 不需 unquote 因 as_uri 直开）
- 检查类任务组（T2/T3）先于修复（T4）：报告先落盘再动手改，禁边查边改
- 改动留痕：T4 每项先红后绿两段输出留档 `_scan` 类临时脚本用后清理，不留垃圾文件于项目根

## 七、验证门（全部满足才算完成，证据形态逐条点名，输出贴进对话）

1. **前置校验**：六项逐条输出一行（版本行实测值/A13 状态/两新文档不存在/索引无 G02/mtime 差值/存档提交号）
2. **T0**：G01 验证门七项证据行全贴（旧词清零/新词落位/重命名 4/死链 0/Playwright 21/21/文档落档/失败清单结论）
3. **T1**：python 断言一行 `移动=6；P3-R01-原型/_demo_ 计数=0；归档 exists=真×6；21页+D04 引用残留=0`
4. **T2**：`grep -c "^| S-" P3-R02-演示场景覆盖梳理.md` → 输出 N≥40 一行；`grep -c "演示动线"` ≥1；汇总行（✅/⚠️/❌ 计数）存在
5. **T3**：报告内 A01~A12 现存 11 份编号 grep 各 ≥1 次命中；`grep -c "^| R-"` ≥1；每条 R 行含「小补|开发性|待拍板」其一与 P0/P1/P2 其一（抽样断言输出）
6. **T4**：执行 R 项数=N；`先红后绿记录=N/N`；`git status --porcelain` 差集 ⊆ 白名单断言输出行；改动页逐页 `PASS <文件名>` 行
7. **T5（如执行）**：`F24 exists=真`；Playwright 打开 0 pageerror/0 console.error；点击登录后 URL 含 F00=真；角色下拉四值与 F21 grep 一致=真；如降级 → 失败清单或 A14 含降级登记行
8. **全量回归**：python Playwright 逐页（21 页；F24 落地则 22 页）`PASS <文件名>` 清单 + 末行 `PASS 21/21 或 22/22, FAIL 0, JS错误 0`
9. **落档**：grep 断言四行——`A14 存在：真`／`原型说明版本行含 V1.7 或含"G02 检查类产出"注记：真`／`PRD 10.2 含 A14 行且含 P3-R02 行且含 D04 行：真`／`_索引 含 G01 行且含 G02 行且两行含 commit 哈希：真`
10. **失败清单**：结论行原文贴出（空清单给「无失败项」）
11. **git**：两段提交哈希两行输出

## 八、执行记录（执行 agent 填写）

- 状态：待执行
- 默认决策命中：（收尾填写）
- 失败清单结论：（收尾填写）
- commit：（收尾填写两段哈希）
